# SDK Product

This folder contains the SDK product shipped to users.

- `plugins`: official SDK DLL plugins such as core, runtime, data, RDB,
  LinkData, debug, and overlay.

These plugins use `plugin-sdk` like any native plugin, but they are part of the
SDK distribution rather than standalone gameplay mods.
