[plugin]
id = "sdk_data"
version = "0.1.0"
entry = "data.dll"

[capabilities]
requires = ["plugin.host", "registry.module"]
provides = ["game.data", "game.characters", "game.missions"]

[registry]
modules = ["sdk.character"]
