[plugin]
id = "sdk_data"
version = "0.1.0"
entry = "data.dll"

[capabilities]
requires = ["plugin.host", "lua.module"]
provides = ["game.data", "game.characters", "game.missions", "std.character.extend"]

[lua]
modules = ["std.character"]
