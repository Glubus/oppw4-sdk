[plugin]
id = "skin_patcher"
version = "0.2.0"
entry = "skin_patcher.dll"

[lua]
modules = ["skin_patcher"]

[capabilities]
requires = ["lua.module", "rdb.patch", "std.character.extend"]
provides = ["std.character.extend", "rdb.skin"]
