[plugin]
id = "sdk_runtime"
version = "0.1.0"
entry = "runtime.dll"

[capabilities]
requires = [
  "plugin.host",
  "config.schema",
]
provides = [
  "game.runtime",
  "game.active_character",
  "game.status",
]
