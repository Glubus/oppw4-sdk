# Native Rust Mod Example

Native Rust mods are experimental. They compile to DLLs and use the registry-first
SDK API, but they require ABI and toolchain discipline that most mod authors do
not need.

Use JavaScript mods unless native code is required.
